const config = {
    API_ENDPOINT: process.env.REACT_APP_API_ENDPOINT || 'http://localhost:8000/api',
    TOKEN_KEY: 'whats-next-client-auth-token',
    ACTIVE_USER: 'whats-next-active-user'
};

export default config;